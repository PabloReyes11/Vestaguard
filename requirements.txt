paho-mqtt>=1.6.1
opencv-python>=4.8.0
numpy>=1.24.0
scikit-learn>=1.3.0
joblib>=1.3.0
# E4: Firebase REST API (equivalente a urequests de MicroPython en la PC)
requests>=2.28.0
# Dependencias Python para VestaGuard â€” Firebase Logger (E4)
# Instalar con: pip install -r requirements_firebase.txt
#
# Nota: se usa 'requests' (Python estandar) en lugar de firebase-admin,
# porque la maestra usa REST API directa (equivalente a urequests en MicroPython).
# Referencia: Sist Progr Unidad4 Firebase.txt â€” seccion "REST API con urequests"

requests>=2.28.0
paho-mqtt>=1.6.1
